# lab4# 📘 Лабораторна робота №4  
## **Тема:** Знайомство з ООП  
## **Мета:**  
Навчитись використовувати основні принципи об’єктно–орієнтованого програмування,  
розглянути конструкції побудови класу, створення об’єктів та навчитись працювати з ними.

---

# 🔧 Хід роботи

## ✔️ Створено файли:
- `main.py` — скрипт з класом та виконанням завдань  
- `lab4.ipynb` — ноутбук з прикладами з лекції  
- `pictures/` — скріншоти (додати свої)

---

# 📌 Код програми `main.py`

```python
class MyName:
    """Опис класу / Документація"""

    total_names = 0  # Class Variable

    def __init__(self, name=None, domain="itcollege.lviv.ua") -> None:
        """Ініціалізація класу"""

        # Перевірка коректності імені
        if name is not None and not name.isalpha():
            raise ValueError("Ім'я може містити лише літери!")

        # Якщо ім'я None — створюємо анонімного користувача
        if name is None:
            obj = self.anonymous_user()
            name = obj.name

        # Форматування: ім'я завжди з великої літери
        self.name = name.capitalize()

        self.domain = domain
        MyName.total_names += 1
        self.my_id = self.total_names

    @property
    def whoami(self) -> str:
        return f"My name is {self.name}"

    @property
    def my_email(self) -> str:
        return self.create_email()

    def create_email(self) -> str:
        """Можна модифікувати домен після @"""
        return f"{self.name.lower()}@{self.domain}"

    @classmethod
    def anonymous_user(cls):
        return cls("Anonymous")

    @staticmethod
    def say_hello(message="Hello to everyone!") -> str:
        return f"You say: {message}"

    @property
    def full_name(self):
        return f"User #{self.my_id}: {self.name} ({self.my_email})"

    def save_to_file(self, filename="users.txt"):
        with open(filename, "a", encoding="utf-8") as f:
            f.write(self.full_name + "\n")
        return f"Saved: {self.full_name}"


print("Розпочинаємо створювати об'єкти!")

names = ("Bohdan", "Marta", None, "Maksym")
all_names = {name: MyName(name) for name in names}

for name, me in all_names.items():
    print(f"""{">*<"*20}
Object: {me} 
Attributes: {me.name} / {me.my_id}
Properties: {me.whoami} / {me.my_email}
Email (instance call): {me.create_email()}
Static method: {me.say_hello("Custom hello!")}
Class variable: from class {MyName.total_names} / from object {me.total_names}
Full name: {me.full_name}
{"<*>"*20}""")

print(f"Ми завершили. Створено {MyName.total_names} об'єктів!")
📤 Результати роботи програми
Додайте власний скріншот запуску програми у консоль.

Приклад вставки:

md
Копіювати код
![Результат виконання](pictures/result.png)
🧩 Відповіді на питання
1️⃣ Чому коли передаємо None — створюється Anonymous?
У __init__ викликається метод anonymous_user(), який повертає MyName("Anonymous").
Тому, якщо ім’я не передано — створюється анонімний користувач.

2️⃣ Як змінити текст привітання в say_hello()?
Метод приймає повідомлення:

python
Копіювати код
me.say_hello("Привіт, світе!")
3️⃣ Функція, що рахує кількість букв імені
python
Копіювати код
@property
def name_length(self):
    return len(self.name)
4️⃣ Чому кількість імен ≠ кількості об’єктів?
Бо None також створює окремий об’єкт "Anonymous".
Тобто об’єктів більше на один.

5️⃣ Ім’я завжди починається з великої літери
Через:

python
Копіювати код
self.name = name.capitalize()
6️⃣ Модифіковано create_email
Домен можна змінювати:

python
Копіювати код
MyName("Maksym", domain="gmail.com")
7️⃣ Перевірка на символи та цифри
python
Копіювати код
if not name.isalpha():
    raise ValueError("Ім'я може містити лише літери!")
8️⃣ Властивість full_name
less
Копіювати код
User #3: Maksym (maksym@itcollege.lviv.ua)
9️⃣ Метод збереження у файл
python
Копіювати код
me.save_to_file("users.txt")
📝 Висновок
✔️ Реалізовано створення класу, об’єктів, властивостей, статичних і класових методів.
✔️ Мета лабораторної досягнута.
✔️ Отримано практичний досвід роботи з ООП.
✔️ Усі завдання виконані, питання — розглянуті.
✔️ Формат здачі через GitHub є зручним.
✔️ Зауважень немає; пропозиція — додати більше практичних задач.

